apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cronicle
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/ryikeda/homelab.git
    targetRevision: main
    path: iac/argocd/cronicle
  destination:
    server: https://kubernetes.default.svc
    namespace: cronicle
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
