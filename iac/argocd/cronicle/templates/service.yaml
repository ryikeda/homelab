apiVersion: v1
kind: Service
metadata:
  name: cronicle
  namespace: {{ .Release.Namespace }}
spec:
  type: ClusterIP
  ports:
    - port: 3012
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: cronicle
