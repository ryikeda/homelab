---
- name: Report host health and inventory facts
  hosts: proxmox:linux:reverse_proxy:dns:gpu:monitoring:storage:container_management:kubernetes
  gather_facts: true
  become: false
  roles:
    - host_info
    - health
