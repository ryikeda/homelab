---
- name: Bootstrap baseline access and common host configuration
  hosts: proxmox:linux:reverse_proxy:dns:gpu:monitoring:container_management:kubernetes
  gather_facts: true
  become: true
  roles:
    - common
    - ssh
    - admin_user
