---
- name: Configure Proxmox hosts
  hosts: proxmox
  gather_facts: true
  become: true
  roles:
    - proxmox_admin_user
    - proxmox_storage
    - proxmox_gpu_passthrough
    - proxmox_vm_template
    - proxmox_lxc_template
    - proxmox_backup
    - proxmox_opentofu_user
