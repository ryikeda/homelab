{
  "identities": [
    {
      "name": "admin",
      "credentials": [
        {
          "accessKey": "{{ seaweedfs_install_s3_access_key }}",
          "secretKey": "{{ seaweedfs_install_s3_secret_key }}"
        }
      ],
      "actions": ["Admin", "Read", "Write"]
    }
  ]
}
