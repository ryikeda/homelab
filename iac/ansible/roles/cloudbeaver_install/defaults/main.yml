---
cloudbeaver_install_dir: "{{ docker_stacks_dir }}/cloudbeaver"

# Bump deliberately - check
# https://hub.docker.com/r/dbeaver/cloudbeaver/tags for the current
# version.
cloudbeaver_install_version: "24.3.0"
cloudbeaver_install_port: 8978
