---
ansible_user: "{{ admin_user_name }}"
ansible_ssh_private_key_file: "{{ ssh_key_path }}"

# Where Dockge and each service's compose.yaml get deployed - see
# dockge_install/jellyfin_install/ollama_install.
docker_stacks_dir: /opt/stacks
