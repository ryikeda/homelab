---
# Rivendell is cloud-init provisioned with the same 'ansible' user + key as
# everything else OpenTofu creates - see vm_rivendell.tf.
ansible_user: "{{ admin_user_name }}"
ansible_ssh_private_key_file: "{{ admin_user_ssh_key_path }}"
